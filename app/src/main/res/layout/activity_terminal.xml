<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#1a1a2e">

    <!-- Terminal header bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="#16213e"
        android:paddingStart="12dp"
        android:paddingEnd="12dp">

        <!-- Session indicator -->
        <TextView
            android:id="@+id/tv_session_info"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="● Shell Active"
            android:textSize="11sp"
            android:textColor="@color/ios_green"
            android:fontFamily="monospace" />

        <!-- Terminal title -->
        <TextView
            android:id="@+id/tv_terminal_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="CodeEditor Terminal v1.3.1"
            android:textSize="12sp"
            android:textColor="#6c7293"
            android:fontFamily="monospace" />
    </LinearLayout>

    <!-- Linux distro control bar -->
    <LinearLayout
        android:id="@+id/linux_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#0f3460"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp">

        <!-- Install buttons (shown when no distro installed) -->
        <LinearLayout
            android:id="@+id/install_buttons"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:visibility="visible">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Install Linux:"
                android:textSize="11sp"
                android:textColor="#8892b0"
                android:fontFamily="monospace"
                android:paddingEnd="8dp" />

            <TextView
                android:id="@+id/btn_install_ubuntu"
                android:layout_width="wrap_content"
                android:layout_height="26dp"
                android:gravity="center"
                android:text="Ubuntu"
                android:textSize="10sp"
                android:textColor="#E95420"
                android:fontFamily="monospace"
                android:background="@drawable/bg_special_key"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:clickable="true"
                android:focusable="true" />

            <TextView
                android:id="@+id/btn_install_debian"
                android:layout_width="wrap_content"
                android:layout_height="26dp"
                android:gravity="center"
                android:text="Debian"
                android:textSize="10sp"
                android:textColor="#A80030"
                android:fontFamily="monospace"
                android:background="@drawable/bg_special_key"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:layout_marginStart="4dp"
                android:clickable="true"
                android:focusable="true" />

            <TextView
                android:id="@+id/btn_install_alpine"
                android:layout_width="wrap_content"
                android:layout_height="26dp"
                android:gravity="center"
                android:text="Alpine"
                android:textSize="10sp"
                android:textColor="#0D597F"
                android:fontFamily="monospace"
                android:background="@drawable/bg_special_key"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:layout_marginStart="4dp"
                android:clickable="true"
                android:focusable="true" />

            <TextView
                android:id="@+id/btn_install_udroid"
                android:layout_width="wrap_content"
                android:layout_height="26dp"
                android:gravity="center"
                android:text="udroid"
                android:textSize="10sp"
                android:textColor="#51cf66"
                android:fontFamily="monospace"
                android:background="@drawable/bg_special_key"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:layout_marginStart="4dp"
                android:clickable="true"
                android:focusable="true" />
        </LinearLayout>

        <!-- Run buttons (shown when distro installed) -->
        <LinearLayout
            android:id="@+id/run_buttons"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:visibility="gone">

            <TextView
                android:id="@+id/btn_start_linux"
                android:layout_width="wrap_content"
                android:layout_height="26dp"
                android:gravity="center"
                android:text="Start Linux"
                android:textSize="10sp"
                android:textColor="#51cf66"
                android:fontFamily="monospace"
                android:background="@drawable/bg_special_key"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:clickable="true"
                android:focusable="true" />

            <TextView
                android:id="@+id/btn_switch_android"
                android:layout_width="wrap_content"
                android:layout_height="26dp"
                android:gravity="center"
                android:text="Android Shell"
                android:textSize="10sp"
                android:textColor="#22b8cf"
                android:fontFamily="monospace"
                android:background="@drawable/bg_special_key"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:layout_marginStart="4dp"
                android:clickable="true"
                android:focusable="true" />
        </LinearLayout>
    </LinearLayout>

    <!-- Separator -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="#2a2a4a" />

    <!-- Terminal WebView -->
    <WebView
        android:id="@+id/webview_terminal"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#1a1a2e" />

    <!-- Separator -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="#2a2a4a" />

    <!-- Special Keys Toolbar - Termux-style -->
    <HorizontalScrollView
        android:id="@+id/special_keys_bar"
        android:layout_width="match_parent"
        android:layout_height="38dp"
        android:background="#0f3460"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="38dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="4dp"
            android:paddingEnd="4dp">

            <!-- ESC -->
            <TextView
                android:id="@+id/btn_esc"
                style="@style/SpecialKeyButton"
                android:text="ESC" />

            <!-- Tab -->
            <TextView
                android:id="@+id/btn_tab"
                style="@style/SpecialKeyButton"
                android:text="TAB" />

            <!-- Ctrl -->
            <TextView
                android:id="@+id/btn_ctrl"
                style="@style/SpecialKeyButton"
                android:text="CTRL" />

            <!-- Arrow keys group -->
            <TextView
                android:id="@+id/btn_arrow_up"
                style="@style/SpecialKeyButton"
                android:text="↑" />

            <TextView
                android:id="@+id/btn_arrow_down"
                style="@style/SpecialKeyButton"
                android:text="↓" />

            <TextView
                android:id="@+id/btn_arrow_left"
                style="@style/SpecialKeyButton"
                android:text="←" />

            <TextView
                android:id="@+id/btn_arrow_right"
                style="@style/SpecialKeyButton"
                android:text="→" />

            <!-- Home / End -->
            <TextView
                android:id="@+id/btn_home"
                style="@style/SpecialKeyButton"
                android:text="HOME" />

            <TextView
                android:id="@+id/btn_end"
                style="@style/SpecialKeyButton"
                android:text="END" />

            <!-- DEL -->
            <TextView
                android:id="@+id/btn_del"
                style="@style/SpecialKeyButton"
                android:text="DEL" />

            <!-- Separator -->
            <View
                android:layout_width="1dp"
                android:layout_height="20dp"
                android:background="#2a2a4a"
                android:layout_marginStart="4dp"
                android:layout_marginEnd="4dp" />

            <!-- Symbols -->
            <TextView
                android:id="@+id/btn_pipe"
                style="@style/SpecialKeyButton"
                android:text="|" />

            <TextView
                android:id="@+id/btn_slash"
                style="@style/SpecialKeyButton"
                android:text="/" />

            <TextView
                android:id="@+id/btn_tilde"
                style="@style/SpecialKeyButton"
                android:text="~" />

            <TextView
                android:id="@+id/btn_dollar"
                style="@style/SpecialKeyButton"
                android:text="$" />

            <TextView
                android:id="@+id/btn_ampersand"
                style="@style/SpecialKeyButton"
                android:text="&amp;" />

            <TextView
                android:id="@+id/btn_semicolon"
                style="@style/SpecialKeyButton"
                android:text=";" />

            <TextView
                android:id="@+id/btn_exclamation"
                style="@style/SpecialKeyButton"
                android:text="!" />

            <TextView
                android:id="@+id/btn_less"
                style="@style/SpecialKeyButton"
                android:text="&lt;" />

            <TextView
                android:id="@+id/btn_greater"
                style="@style/SpecialKeyButton"
                android:text="&gt;" />

            <TextView
                android:id="@+id/btn_quote"
                style="@style/SpecialKeyButton"
                android:text="'" />

            <TextView
                android:id="@+id/btn_double_quote"
                style="@style/SpecialKeyButton"
                android:text="&quot;" />

        </LinearLayout>
    </HorizontalScrollView>

</LinearLayout>
